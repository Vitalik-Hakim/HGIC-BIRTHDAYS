var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
/**
 * Combine 2 payloads, with data in newPayload replacing that in default.
 */
export var mergeInitializePayloads = function (defaultPayload, newPayload) {
    return __assign(__assign(__assign({}, defaultPayload), newPayload), { context: __assign(__assign({}, defaultPayload.context), newPayload.context) });
};
