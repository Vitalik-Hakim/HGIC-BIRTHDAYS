import { AppMessage, PlayerMessage } from "../messages";
/**
 * Send a postMessage to the player.
 */
export declare const sendMessage: (message: AppMessage, targetOrigin: string) => void;
/**
 * Parse the string received from a parent into a typed Message.
 */
export declare const parseMessage: <T>(event: MessageEvent) => PlayerMessage<T>;
